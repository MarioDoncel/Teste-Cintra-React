import { colors } from "./colors";

const theme = {
    color:{
        black:colors.BLACK,
        yellow:colors.YELLOW,
        green:colors.GREEN,
        greenHover:colors.GREEN_HOVER,
        blue:colors.BLUE,
        red:colors.RED
    }
}

export default theme